export default function ExperienceEffects() {
  // v0.17: expensive PCSS soft-shadow shader injection and duplicate AdaptiveDpr were removed.
  // Adaptive DPR is mounted once in MallScene and static shadow maps remain authoritative.
  return null
}
