export default function ExperienceEffects() {
  // v0.17: the expensive shadow shader injection and duplicate DPR helper are intentionally removed.
  // Static shadows plus the single adaptive pixel-density controller remain authoritative.
  return null
}
